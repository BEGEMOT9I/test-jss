import { createUseStyles } from 'react-jss'

export default createUseStyles({
  container: {
    margin: '0.125rem',
    padding: '0.5rem',
    fontSize: '0.75rem',
    color: '#fff',
    backgroundColor: 'red',
    borderRadius: '0.25rem'
  }
})
